#include <stdlib.h>
#include<stdbool.h>
#include "hashtable.h"
#include <assert.h> 
int main(int argc, char const *argv[])
{



    // bool x,y,z;
    // struct HashTable *table=create();
    // unsigned int j= hash("henok");
    // add(table,"henok",1);
    // y=add(table,"teme",2);
    // z=add(table,"josh",3);
    // add(table, "Gehrig",4);
    // add(table, "Mantle",7);
    // unsigned int as=hash("henok");

    // assert( find(table, "Test Key") == NULL);
    // assert( add(table, "Test Key", 11) == true);
    // assert( add(table, "Test Key", 12) == false);
    // assert( add(table, "henok", 12) == false);
    // assert( remove(table, "henok") == true);
    // assert( remove(table, "henokdfdf") == false);
    // assert( find(table, "Test Key") !=NULL);
    // struct Binding* binding =  find(table, "Test Key");
return 0;
}